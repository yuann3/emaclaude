[package]
name = "emaclaude"
version = "0.1.0"
edition = "2024"
rust-version = "1.91"
description = "Claude Code + Doom Emacs orchestration for multi-session workflows"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
